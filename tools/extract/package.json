{
  "name": "@aigate/extract",
  "private": true,
  "type": "module",
  "description": "Dev-only (spec §2): extracts 9router's provider registry into packages/engine/src/catalog. Deleted after SP13.",
  "scripts": {
    "test": "pnpm --filter @aigate/engine build && node --no-warnings --test --test-timeout=60000 test/*.test.mjs"
  },
  "devDependencies": {
    "@aigate/engine": "workspace:*"
  }
}
